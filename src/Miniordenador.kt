/*
class Miniordenador {
    var rato: rato? = null
    private var pantalla: pantalla? = null
    var procesador: cpu? = null

    constructor() {
        rato = rato()
        pantalla = pantalla()
        procesador = cpu()

    }

    constructor(tipo: String, marca: String, pulgadas: Float, velocidade: Int, memoria: Int) {
        rato = rato(tipo)
        pantalla = pantalla(marca, pulgadas)
        procesador = cpu(velocidade, memoria)
    }

    fun setPantalla(pantalla: pantalla) {

    }

    fun getPantalla(): pantalla? {
        return pantalla
    }

    fun amosar() {

        System.out.println("ednaskdnlsasdkn" + rato!!.getTipo() + "a pantalla e" + pantalla!!.getMarca() + "ertdgsgre" + pantalla!!.getPulgadas() + "" + procesador!!.getMemoria() + "" + procesador!!.getVelocidade())

    }

}
*/